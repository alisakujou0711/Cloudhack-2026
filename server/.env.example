PORT=4000

# Set one of these — Gemini is preferred if both are set (override with LLM_PROVIDER).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
ANTHROPIC_API_KEY=

# Optional: force a provider ("gemini" | "anthropic"). Defaults to whichever key is set.
LLM_PROVIDER=
